name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CPPUTEST_HOME: /usr/share/cpputest
  
jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - name: install cpputest
      run: sudo apt install -y cpputest
    - name: Find cpputest
      run: dpkg -L libcpputest-dev
    - uses: actions/checkout@v4
    - name: Run tests
      working-directory: ./Code/Encoder_Controller/tests
      run: make test
